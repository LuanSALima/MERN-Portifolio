import styled from 'styled-components';

export const Container = styled.div`
	display: flex;
	align-items: center;

	input {
		width: 100%;
		margin-right: auto;
	}

	svg {
		margin: 0 10px;
		cursor: pointer;
	}
`;